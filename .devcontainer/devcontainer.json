{
  "name": "btc-quant-bot",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "runServices": ["db"],
  "forwardPorts": [8501],
  "postCreateCommand": "poetry config virtualenvs.in-project true && (test -f .env || cp .env.example .env) && poetry install",
  "postStartCommand": "bash -lc 'mkdir -p logs && nohup bash scripts/run_bot_forever.sh >> logs/bot_supervisor.log 2>&1 &'",
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": ".venv/bin/python",
        "python.analysis.typeCheckingMode": "basic"
      },
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff",
        "ms-azuretools.vscode-docker"
      ]
    }
  }
}
